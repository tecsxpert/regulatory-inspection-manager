server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inspection_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cache:
    type: simple   # ✅ In-memory cache (no Redis)

  mail:
    host: smtp.gmail.com
    port: 587
    username: your_email@gmail.com
    password: your_app_password   # ⚠️ App password only
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: mysupersecretkeythatisverylongandsecure123456
  expiration: 86400000